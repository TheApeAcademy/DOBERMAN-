import { useEffect, useRef } from 'react'
import { useNavigate } from 'react-router-dom'
import { motion } from 'framer-motion'
import { gsap } from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'
import { useShockwave } from '../hooks/useShockwave'
import { AnimatedCounter } from '../components/ui/AnimatedCounter'
import { VideoBlob } from '../components/ui/VideoBlob'
import { SplitReveal } from '../components/ui/SplitReveal'
import { MagneticButton } from '../components/ui/MagneticButton'
import { EyesScene, NoseScene3D, BrainScene3D } from '../components/3d/ModuleScene'

gsap.registerPlugin(ScrollTrigger)

const stats = [
  '400% -- DEEPFAKE ATTACKS UP YoY', '✦',
  '70% -- IoT DEVICES HAVE CRITICAL VULNS', '✦',
  '$4.45M -- AVERAGE DATA BREACH COST 2024', '✦',
  '1 IN 5 MINUTES -- NEW DEEPFAKE CREATED GLOBALLY', '✦',
  '85% -- OF ONLINE CONTENT WILL BE AI-GENERATED BY 2026', '✦',
]

const reviews = [
  { text: 'I uploaded a video of my CEO that turned out to be completely AI-generated. D0B3RMAN caught it in 3 seconds.', author: 'Marcus T.', role: 'IT Security Lead, London', badge: '98% FAKE', badgeColor: 'var(--danger)', rotate: -1.5 },
  { text: 'My home network had 4 critical vulnerabilities I had no idea about. Patched all of them in one afternoon.', author: 'Priya K.', role: 'Remote Software Engineer', badge: 'RISK: 74', badgeColor: 'var(--warning)', rotate: 1.5 },
  { text: 'The news verification alone is worth it. I paste every headline before sharing anything now.', author: 'David O.', role: 'Independent Journalist', badge: '3 FALSE FLAGS', badgeColor: 'var(--danger)', rotate: -1 },
  { text: 'BRAIN explained a zero-day exploit to me like a human being. Not like reading a CVE database. Actually useful.', author: 'Aisha M.', role: 'Cybersecurity Student', badge: 'BRAIN 5/5', badgeColor: 'var(--safe)', rotate: 1 },
  { text: 'Right-clicked a suspicious LinkedIn profile picture. D0B3RMAN said 97% fake. Blocked immediately.', author: 'Tom R.', role: 'Senior Recruiter', badge: '97% FAKE', badgeColor: 'var(--danger)', rotate: -1.5 },
  { text: 'Scanned my entire smart home. Bulbs, TV, cameras, router. Got a full risk report with numbered steps.', author: 'Yuki N.', role: 'Smart Home Enthusiast', badge: '6 VULNS FOUND', badgeColor: 'var(--warning)', rotate: 1.5 },
]

const hPanels = [
  { num: '01', module: 'EYES', headline: 'See\nthrough\nthe fake.', sub: 'Upload any media. Get a trust score in seconds.' },
  { num: '02', module: 'NOSE', headline: 'Sniff out\nevery\nthreat.', sub: 'Your network has secrets. D0B3RMAN finds them.' },
  { num: '03', module: 'BRAIN', headline: 'Ask\nanything.\nKnow it all.', sub: 'A security analyst available every second of every day.' },
  { num: '04', module: 'NEWS', headline: 'Real or\nfake?\nKnow now.', sub: 'Verify any headline or article in seconds.' },
  { num: '05', module: 'EXTENSION', headline: 'Works\neverywhere\nyou do.', sub: 'Right-click any image on the web. Get results instantly.' },
]

export default function Landing() {
  const navigate = useNavigate()
  const shockwave = useShockwave()
  const hContainerRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const ctx = gsap.context(() => {
      // ── Spatial Z-axis reveals: blur + scale + y on scroll ──
      gsap.utils.toArray<HTMLElement>('.z-reveal').forEach((el) => {
        gsap.fromTo(el,
          { opacity: 0, y: 68, scale: 0.93, filter: 'blur(10px)' },
          {
            opacity: 1, y: 0, scale: 1, filter: 'blur(0px)',
            duration: 1.05, ease: 'power3.out',
            scrollTrigger: { trigger: el, start: 'top 88%', toggleActions: 'play none none none' },
          }
        )
      })

      // ── Horizontal pinned scroll panels ──
      const container = hContainerRef.current
      const panels = container ? Array.from(container.querySelectorAll<HTMLElement>('.h-panel')) : []
      if (panels.length > 1) {
        gsap.to(panels, {
          xPercent: -100 * (panels.length - 1),
          ease: 'none',
          scrollTrigger: {
            trigger: container, pin: true, scrub: 1.2,
            snap: { snapTo: 1 / (panels.length - 1), duration: 0.6, ease: 'power2.inOut' },
            end: () => '+=' + panels.length * window.innerWidth,
          },
        })
      }
    })
    return () => ctx.revert()
  }, [])

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0, scale: 1.015, filter: 'blur(8px)' }}
      transition={{ duration: 0.38, ease: [0.25, 0.1, 0.25, 1] }}
      style={{ background: '#000', minHeight: '100vh', overflowX: 'hidden' }}
    >
      {/* ── Responsive overrides ── */}
      <style>{`
        @media (max-width: 767px) {
          .hero-portrait          { width: 100% !important; opacity: 0.72 !important; }
          .hero-portrait-grad     { background: linear-gradient(to right, #000 0%, rgba(0,0,0,0.68) 36%, rgba(0,0,0,0.18) 68%, transparent 90%) !important; }
          .hero-vertical-text     { display: none !important; }
          .hero-version-text      { display: none !important; }
        }
        @media (max-width: 768px) {
          .nav-links, .nav-signin { display: none !important; }
        }
      `}</style>

      {/* ── NAV ── */}
      <nav style={{
        position: 'fixed', top: 12, left: '50%', transform: 'translateX(-50%)',
        width: 'calc(100% - 32px)', maxWidth: 1200,
        padding: '12px 24px',
        background: 'rgba(0,0,0,0.45)', backdropFilter: 'blur(40px)', WebkitBackdropFilter: 'blur(40px)',
        border: '1px solid rgba(255,255,255,0.1)', borderRadius: 14,
        boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.18)',
        zIndex: 200, display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      }}>
        <span style={{ fontFamily: 'Bebas Neue', fontSize: 22, letterSpacing: '0.2em' }}>D0B3RMAN</span>
        <div className="nav-links" style={{ display: 'flex', gap: 40, fontFamily: 'JetBrains Mono', fontSize: 12, color: 'rgba(255,255,255,0.4)' }}>
          {['#modules','#news','#extension','#reviews'].map((href, i) => (
            <a key={i} href={href} style={{ color: 'inherit', textDecoration: 'none' }}>
              {href.slice(1).toUpperCase()}
            </a>
          ))}
        </div>
        <div style={{ display: 'flex', gap: 12 }}>
          <MagneticButton
            onClick={() => navigate('/auth')}
            className="nav-signin"
            style={{ padding: '8px 20px', background: 'rgba(255,255,255,0.06)', backdropFilter: 'blur(20px)', border: '1px solid rgba(255,255,255,0.12)', borderRadius: 8, color: 'white', fontFamily: 'Syne', fontWeight: 600, fontSize: 13, boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.18)' }}
          >
            Sign In
          </MagneticButton>
          <MagneticButton
            onClick={() => navigate('/auth')}
            style={{ padding: '8px 14px', background: 'white', color: 'black', fontFamily: 'Syne', fontWeight: 700, fontSize: 12, border: 'none', borderRadius: 8 }}
          >
            Get Started Free
          </MagneticButton>
        </div>
      </nav>

      {/* ── HERO ── */}
      <section style={{ minHeight: '100vh', position: 'relative', overflow: 'hidden', background: '#000', display: 'flex', alignItems: 'center' }}>

        {/* Dog portrait — shown on ALL screen sizes, mobile overridden via CSS */}
        <motion.div
          className="hero-portrait"
          initial={{ opacity: 0 }} animate={{ opacity: 1 }}
          transition={{ delay: 0.25, duration: 1.4 }}
          style={{ position: 'absolute', right: 0, top: 0, bottom: 0, width: 'clamp(260px, 44%, 580px)', zIndex: 2, pointerEvents: 'none' }}
        >
          <img
            src="/assets/video/b78ad4f230a4015d24a420fce2a7d53b.jpg"
            alt=""
            onError={(e) => { const p = e.currentTarget.parentElement; if (p) p.style.display = 'none' }}
            style={{ width: '100%', height: '100%', objectFit: 'cover', objectPosition: 'center top', display: 'block' }}
          />
          {/* L→R gradient (desktop). CSS overrides to stronger version on mobile. */}
          <div className="hero-portrait-grad" style={{ position: 'absolute', inset: 0, background: 'linear-gradient(to right, #000 0%, rgba(0,0,0,0.5) 30%, transparent 65%)' }} />
          <div style={{ position: 'absolute', bottom: 0, left: 0, right: 0, height: '35%', background: 'linear-gradient(to bottom, transparent, #000)' }} />
        </motion.div>

        <div style={{ position: 'relative', zIndex: 3, padding: 'clamp(80px,12vw,120px) clamp(20px,5vw,48px) 80px', maxWidth: 1200, margin: '0 auto', width: '100%' }}>

          <motion.p
            initial={{ opacity: 0, x: -20 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: 0.15, duration: 0.6 }}
            style={{ fontFamily: 'JetBrains Mono', fontSize: 'clamp(9px, 2.2vw, 11px)', letterSpacing: '0.2em', color: 'rgba(255,255,255,0.25)', marginBottom: 36, textTransform: 'uppercase' }}>
            [ CYBERSECURITY INTELLIGENCE -- 2026 ]
          </motion.p>

          {/* Hero heading — font scales properly from 360px mobile up */}
          <div style={{ position: 'relative', marginBottom: 'clamp(28px, 5vw, 48px)' }}>
            {[
              { word: 'THE', delay: 0.2 },
              { word: 'WATCH-', delay: 0.35 },
              { word: 'DOG.', delay: 0.5 },
            ].map(({ word, delay }) => (
              <motion.div
                key={word}
                initial={{ opacity: 0, y: 80 }} animate={{ opacity: 1, y: 0 }}
                transition={{ delay, duration: 0.9, ease: [0.25, 0.1, 0.25, 1] }}
                style={{
                  fontFamily: 'Syne', fontWeight: 800,
                  fontSize: 'clamp(52px, 14vw, 180px)',
                  lineHeight: 0.88, color: 'white', position: 'relative', zIndex: 1,
                  whiteSpace: 'nowrap',
                }}
              >
                {word}
              </motion.div>
            ))}
          </div>

          <motion.p
            initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.8 }}
            style={{ fontFamily: 'Syne', fontSize: 'clamp(14px, 3.5vw, 17px)', color: 'rgba(255,255,255,0.5)', maxWidth: 460, lineHeight: 1.65, position: 'relative', zIndex: 4 }}>
            Detect deepfakes. Expose IoT vulnerabilities.
            Verify news. Get expert cybersecurity advice.
            Three modules. One platform. Zero guesswork.
          </motion.p>

          <motion.div
            initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 1.0 }}
            style={{ display: 'flex', gap: 'clamp(10px, 3vw, 16px)', marginTop: 'clamp(24px, 5vw, 40px)', flexWrap: 'wrap', position: 'relative', zIndex: 4 }}>
            <MagneticButton
              onClick={(e) => { shockwave(e); navigate('/auth') }}
              style={{ padding: 'clamp(12px,3vw,16px) clamp(24px,7vw,44px)', background: 'white', color: 'black', fontFamily: 'Syne', fontWeight: 700, fontSize: 'clamp(13px, 3vw, 15px)', border: 'none', borderRadius: 12, whiteSpace: 'nowrap' }}
            >
              Deploy Free
            </MagneticButton>
            <MagneticButton
              onClick={() => navigate('/auth')}
              style={{ padding: 'clamp(12px,3vw,16px) clamp(24px,7vw,44px)', background: 'rgba(255,255,255,0.05)', backdropFilter: 'blur(20px)', WebkitBackdropFilter: 'blur(20px)', border: '1px solid rgba(255,255,255,0.15)', borderRadius: 12, color: 'white', fontFamily: 'Syne', fontWeight: 600, fontSize: 'clamp(13px, 3vw, 15px)', boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.18)', whiteSpace: 'nowrap' }}
            >
              See It Work
            </MagneticButton>
          </motion.div>

          <motion.p
            initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.3 }}
            style={{ fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.2)', marginTop: 20, position: 'relative', zIndex: 4 }}>
            Free tier -- no credit card required
          </motion.p>

          <p className="hero-vertical-text" style={{ position: 'absolute', top: 120, right: 0, fontFamily: 'JetBrains Mono', fontSize: 10, color: 'rgba(255,255,255,0.1)', letterSpacing: '0.1em', writingMode: 'vertical-rl', zIndex: 5 }}>
            CYBERSECURITY -- EST. 2026
          </p>
          <p className="hero-version-text" style={{ position: 'absolute', bottom: 40, right: 0, fontFamily: 'JetBrains Mono', fontSize: 10, color: 'rgba(255,255,255,0.1)', letterSpacing: '0.1em', zIndex: 5 }}>
            v1.0.0 -- D0B3RMAN.AI
          </p>
        </div>

        <div style={{ position: 'absolute', bottom: 0, left: 0, right: 0, height: 200, background: 'linear-gradient(to bottom, transparent, #000)', zIndex: 3, pointerEvents: 'none' }} />
      </section>

      {/* ── STATS TICKER ── */}
      <div style={{ overflow: 'hidden', borderTop: '1px solid rgba(255,255,255,0.06)', borderBottom: '1px solid rgba(255,255,255,0.06)', padding: '14px 0', background: 'rgba(6,6,6,0.95)' }}>
        <div style={{ display: 'flex', animation: 'ticker-scroll 35s linear infinite', width: 'max-content' }}>
          {[...stats, ...stats].map((s, i) => (
            <span key={i} style={{ fontFamily: 'JetBrains Mono', fontSize: 'clamp(10px, 2.5vw, 12px)', letterSpacing: '0.12em', color: 'rgba(255,255,255,0.25)', paddingRight: 48, whiteSpace: 'nowrap', textTransform: 'uppercase' }}>
              {s}
            </span>
          ))}
        </div>
      </div>

      {/* ── MODULES ── */}
      <section id="modules" style={{ position: 'relative', overflow: 'hidden', padding: 'clamp(60px,10vw,120px) clamp(20px,4vw,48px)', background: '#000' }}>
        <div style={{ position: 'relative', zIndex: 1, maxWidth: 1200, margin: '0 auto' }}>
          <div style={{ textAlign: 'center', marginBottom: 'clamp(40px, 8vw, 80px)' }}>
            <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.25)', marginBottom: 20 }}>
              [ THREE MODULES. ONE WATCHDOG. ]
            </p>
            <SplitReveal
              text="Intelligence built for today's threats."
              style={{ fontFamily: 'Syne', fontWeight: 800, fontSize: 'clamp(28px, 7vw, 88px)', lineHeight: 0.92, justifyContent: 'center' }}
            />
          </div>

          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(min(100%, 300px), 1fr))', gap: 24 }}>

            {/* EYES */}
            <motion.div
              className="glass z-reveal" data-cursor="view"
              initial={{ opacity: 0, x: -80, rotate: -3 }}
              whileInView={{ opacity: 1, x: 0, rotate: 0 }}
              transition={{ duration: 0.85, ease: [0.25, 0.1, 0.25, 1] }}
              viewport={{ once: true, margin: '-60px' }}
              whileHover={{ y: -8, transition: { duration: 0.2 } }}
              style={{ padding: 'clamp(20px, 4vw, 36px)', borderRadius: 24, position: 'relative', overflow: 'hidden' }}
            >
              <div style={{ position: 'relative', height: 'clamp(180px, 30vw, 260px)', borderRadius: 16, overflow: 'hidden', marginBottom: 28, background: '#060606' }}>
                <EyesScene />
                <div style={{ position: 'absolute', bottom: 14, left: 16, fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.5)', zIndex: 2 }}>
                  EYES -- DEEPFAKE DETECTION
                </div>
              </div>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.3)', marginBottom: 12 }}>01 -- DEEPFAKE DETECTION</p>
              <h3 style={{ fontFamily: 'Bebas Neue', fontSize: 'clamp(36px, 7vw, 56px)', letterSpacing: '0.1em', marginBottom: 16 }}>EYES</h3>
              <p style={{ color: 'rgba(255,255,255,0.5)', fontSize: 'clamp(13px, 3vw, 15px)', lineHeight: 1.65, marginBottom: 24 }}>
                Upload any image, video, or audio. D0B3RMAN's detection engine analyzes it
                against known deepfake signatures and returns a trust score in seconds.
              </p>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.2)' }}>Hive AI - XceptionNet - EfficientNet - MesoNet</p>
            </motion.div>

            {/* NOSE */}
            <motion.div
              className="glass z-reveal" data-cursor="view"
              initial={{ opacity: 0, y: 80, scale: 0.94 }}
              whileInView={{ opacity: 1, y: 0, scale: 1 }}
              transition={{ duration: 0.85, delay: 0.12, ease: [0.25, 0.1, 0.25, 1] }}
              viewport={{ once: true, margin: '-60px' }}
              whileHover={{ y: -8, transition: { duration: 0.2 } }}
              style={{ padding: 'clamp(20px, 4vw, 36px)', borderRadius: 24, position: 'relative', overflow: 'hidden' }}
            >
              <div style={{ position: 'relative', height: 'clamp(180px, 30vw, 260px)', borderRadius: 16, overflow: 'hidden', marginBottom: 28, background: '#060606' }}>
                <NoseScene3D />
                <div style={{ position: 'absolute', bottom: 14, left: 16, fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.5)', zIndex: 2 }}>
                  NOSE -- NETWORK INTELLIGENCE
                </div>
              </div>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.3)', marginBottom: 12 }}>02 -- NETWORK INTELLIGENCE</p>
              <h3 style={{ fontFamily: 'Bebas Neue', fontSize: 'clamp(36px, 7vw, 56px)', letterSpacing: '0.1em', marginBottom: 16 }}>NOSE</h3>
              <p style={{ color: 'rgba(255,255,255,0.5)', fontSize: 'clamp(13px, 3vw, 15px)', lineHeight: 1.65, marginBottom: 24 }}>
                Describe your network environment. D0B3RMAN identifies device vulnerabilities,
                maps real CVEs, and gives you a prioritized action plan.
              </p>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.2)' }}>NIST NVD - Real CVE Mapping - Device Fingerprinting</p>
            </motion.div>

            {/* BRAIN */}
            <motion.div
              className="glass z-reveal" data-cursor="view"
              initial={{ opacity: 0, x: 80, rotate: 3 }}
              whileInView={{ opacity: 1, x: 0, rotate: 0 }}
              transition={{ duration: 0.85, delay: 0.25, ease: [0.25, 0.1, 0.25, 1] }}
              viewport={{ once: true, margin: '-60px' }}
              whileHover={{ y: -8, transition: { duration: 0.2 } }}
              style={{ padding: 'clamp(20px, 4vw, 36px)', borderRadius: 24, position: 'relative', overflow: 'hidden' }}
            >
              <div style={{ position: 'relative', height: 'clamp(180px, 30vw, 260px)', borderRadius: 16, overflow: 'hidden', marginBottom: 28, background: '#060606' }}>
                <BrainScene3D />
                <div style={{ position: 'absolute', bottom: 14, left: 16, fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.5)', zIndex: 2 }}>
                  BRAIN -- AI SECURITY ANALYST
                </div>
              </div>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.3)', marginBottom: 12 }}>03 -- AI SECURITY ANALYST</p>
              <h3 style={{ fontFamily: 'Bebas Neue', fontSize: 'clamp(36px, 7vw, 56px)', letterSpacing: '0.1em', marginBottom: 16 }}>BRAIN</h3>
              <p style={{ color: 'rgba(255,255,255,0.5)', fontSize: 'clamp(13px, 3vw, 15px)', lineHeight: 1.65, marginBottom: 24 }}>
                Ask anything. Upload suspicious files. D0B3RMAN explains threats in plain language
                and gives you a concrete next step. Like having a security analyst on call 24/7.
              </p>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.2)' }}>Claude Sonnet - Context-aware - File Upload Support</p>
            </motion.div>

          </div>
        </div>
      </section>

      {/* ── STATS ROW ── */}
      <div style={{ padding: 'clamp(48px,8vw,80px) clamp(20px,4vw,48px)', borderTop: '1px solid rgba(255,255,255,0.05)', borderBottom: '1px solid rgba(255,255,255,0.05)', background: 'rgba(6,6,6,0.98)' }}>
        <div style={{ maxWidth: 1200, margin: '0 auto', display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(min(100%, 180px), 1fr))', gap: 40, textAlign: 'center' }}>
          {[
            { target: 400, suffix: '%', label: 'Rise in deepfake attacks' },
            { target: 70, suffix: '%', label: 'IoT devices with critical vulns' },
            { target: 4, suffix: 'M+', prefix: '$', label: 'Average breach cost' },
            { target: 3, suffix: ' modules', label: 'One platform' },
          ].map((stat, i) => (
            <motion.div
              key={i} className="z-reveal"
              initial={{ opacity: 0, y: 48 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: i * 0.1, ease: [0.25, 0.1, 0.25, 1] }}
              viewport={{ once: true, margin: '-60px' }}
            >
              <div style={{ fontFamily: 'Syne', fontWeight: 800, fontSize: 'clamp(32px, 5vw, 64px)', lineHeight: 1, marginBottom: 8 }}>
                <AnimatedCounter target={stat.target} prefix={stat.prefix} suffix={stat.suffix} />
              </div>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 'clamp(9px, 2vw, 11px)', color: 'rgba(255,255,255,0.25)', letterSpacing: '0.1em' }}>
                {stat.label.toUpperCase()}
              </p>
            </motion.div>
          ))}
        </div>
      </div>

      {/* ── HORIZONTAL PINNED SCROLL ── */}
      <div ref={hContainerRef} className="h-container" style={{ overflow: 'hidden' }}>
        <div style={{ display: 'flex', width: `${hPanels.length * 100}vw`, height: '100vh' }}>
          {hPanels.map((panel, i) => (
            <div
              key={i} className="h-panel"
              style={{ width: '100vw', height: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', position: 'relative', overflow: 'hidden', background: '#000' }}
            >
              <VideoBlob src="/assets/video/blob-news.mp4" cover opacity={0.25} style={{ zIndex: 0 }} />
              <div className="glass" style={{ position: 'relative', zIndex: 1, padding: 'clamp(24px,6vw,60px) clamp(20px,7vw,72px)', borderRadius: 32, maxWidth: 600, textAlign: 'center', margin: '0 20px' }}>
                <p style={{ fontFamily: 'JetBrains Mono', fontSize: 'clamp(9px, 2vw, 11px)', letterSpacing: '0.25em', color: 'rgba(255,255,255,0.25)', marginBottom: 20 }}>
                  {panel.num} -- {panel.module}
                </p>
                <h2 style={{ fontFamily: 'Syne', fontWeight: 800, fontSize: 'clamp(36px, 8vw, 96px)', lineHeight: 0.9, whiteSpace: 'pre-line', marginBottom: 32 }}>
                  {panel.headline}
                </h2>
                <p style={{ fontFamily: 'Syne', fontSize: 'clamp(14px, 3vw, 18px)', color: 'rgba(255,255,255,0.5)', lineHeight: 1.6 }}>
                  {panel.sub}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* ── BLOB DIVIDER ── */}
      <div style={{ position: 'relative', height: 'clamp(120px, 20vw, 240px)', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#000' }}>
        <VideoBlob src="/assets/video/blob-divider.mp4" cover opacity={0.4} style={{ zIndex: 0 }} />
        <span style={{ position: 'relative', zIndex: 1, fontFamily: 'Bebas Neue', fontSize: 'clamp(40px, 12vw, 140px)', letterSpacing: '0.25em', color: 'rgba(255,255,255,0.03)', pointerEvents: 'none', userSelect: 'none' }}>
          D0B3RMAN
        </span>
      </div>

      {/* ── NEWS SECTION ── */}
      <section id="news" style={{ position: 'relative', overflow: 'hidden', padding: 'clamp(60px,10vw,100px) clamp(20px,4vw,48px)', background: '#000' }}>
        <div style={{ position: 'relative', zIndex: 1, maxWidth: 1200, margin: '0 auto' }}>
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: 'clamp(32px, 6vw, 60px)', alignItems: 'center' }}>
            <motion.div className="z-reveal" style={{ flex: '1 1 min(100%, 300px)' }}>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, letterSpacing: '0.2em', color: 'var(--danger)', marginBottom: 20 }}>
                [ FAKE NEWS DETECTION ]
              </p>
              <SplitReveal
                text="Don't believe everything you read."
                style={{ fontFamily: 'Syne', fontWeight: 800, fontSize: 'clamp(28px, 7vw, 88px)', lineHeight: 0.92, marginBottom: 20 }}
              />
              <p style={{ color: 'rgba(255,255,255,0.5)', fontSize: 'clamp(13px, 3vw, 16px)', maxWidth: 500, lineHeight: 1.65, marginBottom: 36 }}>
                Paste any headline, claim, or article URL. D0B3RMAN cross-references it,
                checks source quality, and gives you a credibility verdict in seconds.
              </p>
              <MagneticButton
                onClick={() => navigate('/auth')}
                style={{ padding: 'clamp(10px,2.5vw,14px) clamp(20px,4vw,36px)', background: 'white', color: 'black', fontFamily: 'Syne', fontWeight: 700, fontSize: 'clamp(12px, 2.5vw, 14px)', border: 'none', borderRadius: 10 }}
              >
                Try News Verification
              </MagneticButton>
            </motion.div>

            <motion.div
              className="glass z-reveal"
              style={{ flex: '1 1 min(100%, 300px)', padding: 'clamp(24px,5vw,40px)', borderRadius: 24, position: 'relative', overflow: 'hidden', minHeight: 'clamp(240px, 40vw, 360px)', display: 'flex', flexDirection: 'column', justifyContent: 'flex-end' }}
            >
              <VideoBlob src="/assets/video/blob-news.mp4" cover opacity={0.4} style={{ zIndex: 0 }} />
              <div style={{ position: 'relative', zIndex: 1 }}>
                <p style={{ fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.15em', color: 'var(--danger)', marginBottom: 16 }}>VERDICT -- LIKELY FALSE</p>
                <p style={{ fontFamily: 'Syne', fontWeight: 700, fontSize: 'clamp(14px, 3vw, 18px)', lineHeight: 1.4, marginBottom: 12 }}>"Breaking: Scientists Confirm AI Will Replace All Jobs By 2025"</p>
                <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
                  {['Sensationalist framing', 'No primary source', 'Vague attribution'].map((flag) => (
                    <span key={flag} style={{ fontFamily: 'JetBrains Mono', fontSize: 10, color: 'var(--danger)', background: 'rgba(255,45,45,0.12)', border: '1px solid rgba(255,45,45,0.25)', padding: '4px 10px', borderRadius: 4 }}>{flag}</span>
                  ))}
                </div>
              </div>
            </motion.div>
          </div>
        </div>
      </section>

      {/* ── EXTENSION SECTION ── */}
      <section id="extension" style={{ padding: 'clamp(60px,10vw,100px) clamp(20px,4vw,48px)', borderTop: '1px solid rgba(255,255,255,0.05)', background: 'rgba(4,4,4,0.98)' }}>
        <div style={{ maxWidth: 1200, margin: '0 auto' }}>
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: 'clamp(32px, 6vw, 60px)', alignItems: 'center' }}>
            <motion.div className="z-reveal" style={{ flex: '1 1 min(100%, 300px)' }}>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.25)', marginBottom: 20 }}>[ BROWSER EXTENSION ]</p>
              <SplitReveal
                text="D0B3RMAN everywhere you browse."
                style={{ fontFamily: 'Syne', fontWeight: 800, fontSize: 'clamp(28px, 7vw, 88px)', lineHeight: 0.92, marginBottom: 20 }}
              />
              <p style={{ color: 'rgba(255,255,255,0.5)', fontSize: 'clamp(13px, 3vw, 16px)', maxWidth: 500, lineHeight: 1.65, marginBottom: 36 }}>
                Right-click any image, video, or selected text on any website.
                Results appear in a floating panel — without leaving the page.
              </p>
              <div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', marginBottom: 36 }}>
                {['Right-click any image', 'Selected text verification', 'Works on all sites'].map((feat) => (
                  <span key={feat} style={{ padding: '8px 14px', background: 'rgba(255,255,255,0.04)', backdropFilter: 'blur(20px)', border: '1px solid rgba(255,255,255,0.08)', borderRadius: 8, fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.4)', boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.08)' }}>
                    {feat}
                  </span>
                ))}
              </div>
              <MagneticButton
                style={{ padding: 'clamp(10px,2.5vw,14px) clamp(20px,4vw,36px)', background: 'white', color: 'black', fontFamily: 'Syne', fontWeight: 700, fontSize: 'clamp(12px, 2.5vw, 14px)', border: 'none', borderRadius: 10 }}
              >
                Add to Chrome — Free
              </MagneticButton>
            </motion.div>

            <motion.div
              className="glass z-reveal"
              style={{ flex: '1 1 min(100%, 300px)', padding: 'clamp(20px,4vw,32px)', borderRadius: 24, minHeight: 'clamp(280px, 45vw, 400px)', display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}
            >
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.15em', color: 'rgba(255,255,255,0.25)', marginBottom: 24 }}>
                EXTENSION PREVIEW
              </div>
              <div style={{ padding: 'clamp(16px, 3vw, 24px)', background: 'rgba(6,6,6,0.94)', backdropFilter: 'blur(28px)', WebkitBackdropFilter: 'blur(28px)', border: '1px solid rgba(255,255,255,0.1)', borderRadius: 20, boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.18)' }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16 }}>
                  <span style={{ fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.4)' }}>D0B3RMAN</span>
                  <span style={{ color: 'rgba(255,255,255,0.3)', fontSize: 18 }}>×</span>
                </div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 'clamp(40px, 10vw, 64px)', fontWeight: 700, color: 'var(--danger)', lineHeight: 1, marginBottom: 4 }}>97<span style={{ fontSize: 28 }}>%</span></div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 11, letterSpacing: '0.15em', color: 'var(--danger)', marginBottom: 12 }}>FAKE</div>
                <p style={{ fontFamily: 'Syne', fontSize: 'clamp(11px, 2.5vw, 12px)', color: 'rgba(255,255,255,0.5)', lineHeight: 1.55, marginBottom: 16 }}>
                  This image shows strong deepfake indicators. Facial boundaries and lighting are inconsistent.
                </p>
                <div style={{ textAlign: 'center', padding: '10px', background: 'white', color: 'black', borderRadius: 10, fontFamily: 'JetBrains Mono', fontSize: 12, fontWeight: 700 }}>
                  VIEW FULL REPORT
                </div>
              </div>
            </motion.div>
          </div>
        </div>
      </section>

      {/* ── BLOB DIVIDER 2 ── */}
      <div style={{ position: 'relative', height: 'clamp(120px, 20vw, 240px)', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#000' }}>
        <VideoBlob src="/assets/video/blob-cta.mp4" cover opacity={0.35} style={{ zIndex: 0 }} />
        <span style={{ position: 'relative', zIndex: 1, fontFamily: 'Bebas Neue', fontSize: 'clamp(40px, 12vw, 140px)', letterSpacing: '0.25em', color: 'rgba(255,255,255,0.03)', userSelect: 'none' }}>
          SECURITY
        </span>
      </div>

      {/* ── REVIEWS ── */}
      <section id="reviews" style={{ padding: 'clamp(60px,8vw,100px) 0 clamp(60px,8vw,100px) clamp(20px,4vw,48px)', overflow: 'hidden', background: '#000' }}>
        <motion.div className="z-reveal" style={{ maxWidth: 1200, marginBottom: 56 }}>
          <motion.div
            initial={{ opacity: 0, scale: 0.96 }}
            whileInView={{ opacity: 1, scale: 1 }}
            transition={{ duration: 1.0, ease: [0.25, 0.1, 0.25, 1] }}
            viewport={{ once: true }}
            style={{ margin: '0 auto 40px', borderRadius: 20, overflow: 'hidden', width: '100%', maxWidth: 1120, height: 'clamp(240px, 50vw, 720px)', position: 'relative' }}
          >
            <img
              src="/assets/video/b78ad4f230a4015d24a420fce2a7d53b.jpg"
              alt="Doberman"
              style={{ width: '100%', height: '100%', objectFit: 'cover', objectPosition: 'center top', display: 'block' }}
            />
            <div style={{ position: 'absolute', bottom: 0, left: 0, right: 0, height: '40%', background: 'linear-gradient(to bottom, transparent, #000)' }} />
          </motion.div>
          <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.25)', marginBottom: 20 }}>[ WHAT USERS SAY ]</p>
          <SplitReveal
            text="The dog delivers."
            style={{ fontFamily: 'Syne', fontWeight: 800, fontSize: 'clamp(32px, 7vw, 88px)', lineHeight: 0.92 }}
          />
        </motion.div>

        <motion.div
          drag="x"
          dragConstraints={{ right: 0, left: -(reviews.length * 380) }}
          style={{ display: 'flex', gap: 20, width: 'max-content', cursor: 'grab' }}
        >
          {reviews.map((r, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              viewport={{ once: true }}
              whileHover={{ y: -8, rotate: 0, transition: { duration: 0.2 } }}
              style={{
                width: 'clamp(280px, 80vw, 360px)', padding: 32, flexShrink: 0,
                position: 'relative', overflow: 'hidden', rotate: r.rotate,
                background: 'rgba(255,255,255,0.04)', backdropFilter: 'blur(40px)', WebkitBackdropFilter: 'blur(40px)',
                border: '1px solid rgba(255,255,255,0.10)', borderRadius: 20,
                boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.18), 0 24px 48px rgba(0,0,0,0.5)',
              }}
            >
              <span style={{ position: 'absolute', top: 20, right: 20, fontFamily: 'JetBrains Mono', fontSize: 10, letterSpacing: '0.15em', color: r.badgeColor, background: `${r.badgeColor}22`, border: `1px solid ${r.badgeColor}44`, padding: '3px 8px', borderRadius: 4 }}>
                {r.badge}
              </span>
              <p style={{ fontFamily: 'Syne', fontSize: 'clamp(13px, 2.5vw, 15px)', color: 'rgba(255,255,255,0.55)', lineHeight: 1.65, marginBottom: 24, marginTop: 8 }}>"{r.text}"</p>
              <p style={{ fontFamily: 'Syne', fontWeight: 700, fontSize: 14, color: 'white' }}>{r.author}</p>
              <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.25)', marginTop: 4 }}>{r.role}</p>
            </motion.div>
          ))}
        </motion.div>
      </section>

      {/* ── FINAL CTA ── */}
      <section style={{ minHeight: '100vh', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', position: 'relative', overflow: 'hidden', background: '#000' }}>
        <VideoBlob src="/assets/video/blob-cta.mp4" cover opacity={0.5} style={{ zIndex: 0 }} />
        <div style={{ position: 'relative', zIndex: 1, padding: '0 clamp(20px, 5vw, 24px)', display: 'flex', flexDirection: 'column', alignItems: 'center', width: '100%', maxWidth: 720 }}>
          <div style={{ position: 'relative', height: 'clamp(200px, 40vw, 480px)', width: '100%', marginBottom: -60, overflow: 'visible', pointerEvents: 'none', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
            <video
              autoPlay muted loop playsInline preload="auto"
              style={{ width: '90%', height: '100%', objectFit: 'contain', mixBlendMode: 'screen', transform: 'scale(1.4)', transformOrigin: 'center center' }}
            >
              <source src="/assets/video/blob-hero-2.mp4" type="video/mp4" />
            </video>
          </div>
          <motion.div
            initial={{ opacity: 0, scale: 0.94, y: 40 }}
            whileInView={{ opacity: 1, scale: 1, y: 0 }}
            transition={{ duration: 0.9, ease: [0.25, 0.1, 0.25, 1] }}
            viewport={{ once: true }}
            className="glass"
            style={{ padding: 'clamp(32px,8vw,90px) clamp(20px,6vw,72px)', borderRadius: 36, textAlign: 'center', width: '100%' }}
          >
            <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, letterSpacing: '0.2em', color: 'rgba(255,255,255,0.25)', marginBottom: 28 }}>
              THE WATCHDOG IS READY
            </p>
            <SplitReveal
              text="Deploy D0B3RMAN."
              style={{ fontFamily: 'Syne', fontWeight: 800, fontSize: 'clamp(32px, 9vw, 110px)', lineHeight: 0.92, marginBottom: 36, justifyContent: 'center' }}
            />
            <p style={{ fontFamily: 'Syne', fontSize: 'clamp(14px, 3vw, 17px)', color: 'rgba(255,255,255,0.5)', marginBottom: 'clamp(32px, 6vw, 52px)' }}>
              Free tier. No credit card. No limits on intelligence.
            </p>
            <MagneticButton
              onClick={(e) => { shockwave(e); navigate('/auth') }}
              style={{ padding: 'clamp(14px,3vw,20px) clamp(36px,8vw,72px)', background: 'white', color: 'black', fontFamily: 'Syne', fontWeight: 700, fontSize: 'clamp(14px, 3vw, 17px)', border: 'none', borderRadius: 14 }}
            >
              Get Started Free
            </MagneticButton>
            <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.2)', marginTop: 20 }}>
              3 free scans per module per day -- no card required
            </p>
          </motion.div>
        </div>
      </section>

      {/* ── FOOTER ── */}
      <footer style={{ borderTop: '1px solid rgba(255,255,255,0.06)', padding: 'clamp(20px,4vw,32px) clamp(20px,4vw,48px)', display: 'flex', alignItems: 'center', justifyContent: 'space-between', background: 'rgba(4,4,4,0.98)', flexWrap: 'wrap', gap: 20 }}>
        <span style={{ fontFamily: 'Bebas Neue', fontSize: 18, letterSpacing: '0.2em' }}>D0B3RMAN</span>
        <div style={{ display: 'flex', gap: 32, fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.2)' }}>
          <a href="/privacy" style={{ color: 'inherit', textDecoration: 'none' }}>Privacy</a>
          <a href="/terms" style={{ color: 'inherit', textDecoration: 'none' }}>Terms</a>
          <a href="mailto:hello@d0b3rman.ai" style={{ color: 'inherit', textDecoration: 'none' }}>Contact</a>
        </div>
        <p style={{ fontFamily: 'JetBrains Mono', fontSize: 11, color: 'rgba(255,255,255,0.2)' }}>2026 D0B3RMAN. All rights reserved.</p>
      </footer>

    </motion.div>
  )
}
